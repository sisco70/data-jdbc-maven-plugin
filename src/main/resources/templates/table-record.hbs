package {{packageName}};

{{#if remarksAvailable~}}
import io.swagger.v3.oas.annotations.media.Schema;
{{/if~}}
{{#if validationAvailable~}}
import jakarta.validation.constraints.*;
{{/if~}}
{{#if pkColumns~}}
import org.springframework.data.annotation.Id;
{{/if~}}
{{#if hasCustomTableMapping~}}
import org.springframework.data.relational.core.mapping.Table;
{{/if~}}
{{#if hasCustomFieldMappings~}}
import org.springframework.data.relational.core.mapping.Column;
{{/if~}}
{{#each imports~}}
import {{this}};
{{/each}}

/**
* Autogenerated by data-jdbc-maven-plugin on {{generationDate}}
*/
{{#if hasCustomTableMapping~}}
    @Table("{{dbTableName}}")
{{/if~}}
{{#if remarksAvailable~}}
    @Schema(description = "{{dbTableComment}}")
{{/if~}}
public record {{className}}(
{{~#if pkColumns}}
    {{#if hasCompositePk}}@Id {{className}}Id id{{else~}}
    {{#each pkColumns}}{{#if notNull}}@NotNull
    {{/if~}}
    {{#if stringSize}}@Size(min = 1, max = {{stringSize}}, message = "{{javaName}} must be between 1 and {{stringSize}} characters")
    {{/if~}}
    {{#if dbColComment}}@Schema(description = "{{{dbColComment}}}")
    {{/if~}}
    @Id {{#if hasCustomMapping}}@Column("{{dbName}}") {{/if}}{{type}} {{javaName}}{{/each}}{{/if~}}
    {{#if columns}},
    {{/if}}
{{~/if}}
{{~#each columns}}
    {{#if notNull}}@NotNull
    {{/if~}}
    {{#if stringSize}}@Size(min = 1, max = {{stringSize}}, message = "{{javaName}} must be between 1 and {{stringSize}} characters")
    {{/if~}}
    {{#if dbColComment}}@Schema(description = "{{{dbColComment}}}")
    {{/if~}}
    {{#if hasCustomMapping}}@Column("{{dbName}}") {{/if}}{{type}} {{javaName}}{{#unless @last}},
    {{/unless~}}
{{/each}}
) {
{{~#if hasCompositePk}}
    public record {{className}}Id(
    {{~#each pkColumns}}
        {{#if notNull}}@NotNull
        {{/if~}}
        {{#if stringSize}}@Size(min = 1, max = {{stringSize}}, message = "{{javaName}} must be between 1 and {{stringSize}} characters")
        {{/if~}}
        {{#if dbColComment}}@Schema(description = "{{{dbColComment}}}")
        {{/if~}}
        {{#if hasCustomMapping}}@Column("{{dbName}}") {{/if}}{{type}} {{javaName}}{{#unless @last}},
        {{/unless~}}
    {{/each~}}
    ) {}
{{/if~}}
}